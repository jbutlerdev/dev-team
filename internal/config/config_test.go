package config

import "testing"

func TestLoadConfig(t *testing.T) {
	// Add test cases here to verify the LoadConfig function.
	// Example:
	// cfg, err := LoadConfig(".env")
	// if err != nil {
	//  t.Fatalf("Failed to load config: %v", err)
	// }
	// if cfg.Port != 8080 {
	//  t.Errorf("Expected port 8080, got %d", cfg.Port)
	// }
}
